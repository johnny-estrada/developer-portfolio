---
---

<section class="hero">
  <div class="container hero__container">
    <div class="img-wrapper">
      <!-- <image class="hero__img" src="/images/hero-image-mobile.webp" alt="" /> -->
      <image class="hero__img" src="/images/hero-image-mobile.webp" alt="A black and white picture of Johnny Estrada smilling, with his arms crossed." width="174px" min-height="383px" />
    </div>

      <h1>Nice to meet you! I'm Johnny.</h1>
      <p>
        Based in the San Diego, I'm a front-end developer passionate about
        building accessible web apps that users love.
      </p>
      <a href="/contact">LETS CHAT</a>

  </div>
  <div class="bg-pattern-rings"></div>
  <div class="bg-pattern-circle"></div>
</section>

<style lang="scss">
  .hero {

    .img-wrapper {
      width: 174px;
      height: 325px;
      margin-bottom: 40px;
    }

    &__img {
      position: absolute;
      object-fit: contain;
      top: 0;
      z-index: -1;
    }

    &__container {
      padding: 0 16px;
      border-bottom: var(--white) solid 1px;
    }

    .bg-pattern-rings {
      background-image: url("/icons/pattern-rings.svg");
      background-repeat: no-repeat;
      width: 530px;
      height: 129px;
      position: absolute;
      left: -310px;
      top: 120px;
      z-index: -2;
    }

    .bg-pattern-circle {
      background-image: url("/icons/pattern-circle.svg");
      background-repeat: no-repeat;
      position: absolute;
      height: 129px;
      right: 0;
      left: 80%;
      top: 254px;
      z-index: 1;
    }

    h1 {
      font-weight: 700;
      font-size: 40px;
      line-height: 40px;

      text-align: center;
      letter-spacing: -1.13636px;
      margin-bottom: 24px;
    }

    p {
      font-size: 16px;
      line-height: 26px;
      color: #D9D9D9;
      text-align: center;
      margin-bottom: 36px;
    }

    a {
      cursor: pointer;
      display: flex;
      justify-content: center;
      align-items: center;
      width: 250px;
      height: 56px;
      border: 2px solid #4ee1a0;
      box-shadow: 5px 8px rgba(78, 225, 160, 0.25);
      border-radius: 50px;
      margin-bottom: 80px;

      &:hover {
      background-color: var(--accent-green-400);
      box-shadow: 0px 0px 50px 1px rgba(78, 225, 160, 0.25);
      translate: 2px 2px;
      transition: all 300ms ease-in-out;
    }
    &:focus {
      border: none;
      background-color: var(--accent-green-400);
      box-shadow: 0px 0px 50px 1px rgba(78, 225, 160, 0.25);
    }
    &:active {
      border: none;
      color: #212121;
      background-color: white;
    }
    }
  }
</style>
